BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "resin-generic"
BBFILE_PATTERN_resin-generic := "^${LAYERDIR}/"
BBFILE_PRIORITY_resin-generic = "1337"
